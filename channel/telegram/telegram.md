## Telegram Rules

- Telegram supports Markdown formatting — use it for code blocks, bold, and italic.
- Users cannot access server local files; include file paths so the system sends them as attachments.
- Keep messages under 4000 characters; the system auto-splits longer messages.
