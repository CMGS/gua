name: build

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: darwin
            goarch: amd64
          - goos: darwin
            goarch: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          VERSION=$(git describe --tags $(git rev-list --tags --max-count=1) 2>/dev/null || echo dev)
          REVISION=$(git rev-parse HEAD)
          BUILTAT=$(date +%Y-%m-%dT%H:%M:%S)
          LDFLAGS="-s -X github.com/CMGS/gua/version.VERSION=${VERSION} \
                   -X github.com/CMGS/gua/version.REVISION=${REVISION} \
                   -X github.com/CMGS/gua/version.BUILTAT=${BUILTAT}"
          go build -ldflags "${LDFLAGS}" -o gua-server-${{ matrix.goos }}-${{ matrix.goarch }} ./cmd/
          go build -ldflags "-s" -o gua-bridge-${{ matrix.goos }}-${{ matrix.goarch }} ./agent/claude/bridge/

      - name: Upload artifacts
        uses: actions/upload-artifact@v6
        with:
          name: gua-${{ matrix.goos }}-${{ matrix.goarch }}
          path: |
            gua-server-${{ matrix.goos }}-${{ matrix.goarch }}
            gua-bridge-${{ matrix.goos }}-${{ matrix.goarch }}
          retention-days: 30
